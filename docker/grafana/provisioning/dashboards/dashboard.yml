# Provisions dashboards from JSON files placed in this directory.
apiVersion: 1
providers:
  - name: "default"
    folder: "Margin"
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards
